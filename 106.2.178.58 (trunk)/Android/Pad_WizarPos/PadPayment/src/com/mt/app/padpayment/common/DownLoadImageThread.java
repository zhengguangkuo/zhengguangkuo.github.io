package com.mt.app.padpayment.common;

import java.util.List;

import com.mt.android.protocol.http.client.ImageHttpClient;


public class DownLoadImageThread extends Thread{/*
	   List<AppListRespBean> list = null;
       public  DownLoadImageThread(List<AppListRespBean> list){
    	   this.list = list;
       }
       
       public void run(){
    	  
    	  for(int i = 0; i < list.size(); i ++){
    		  AppListRespBean app = (AppListRespBean)list.get(i);
    		  ImageHttpClient.saveImage(app.getImagePath(), app.getAppIdent());  
    	  }
       }
*/}
